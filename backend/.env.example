# Database
DB_HOST=localhost
DB_PORT=3306
DB_NAME=nexgadget
DB_USER=root
DB_PASS=

# JWT secret – change to a strong random string in production
JWT_SECRET=nexgadget_jwt_secret_change_in_production_2024

# App URL – leave empty to auto-detect from request
APP_URL=

# CORS origin – set to your frontend URL in production (e.g. https://example.com)
CORS_ORIGIN=*
